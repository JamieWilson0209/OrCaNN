segment output: labels, centroids, per-ROI traces

  segment_outputs_<run key>/
      <recording>/   data/     labels.npy  centroids.npy  traces.npy
                               max_projection.npy  meta.json
                     figures/  overlay.png

Each meta.json records the threshold and radius the recording was extracted
under and the digest of the infer output it read, so changing either makes those
recordings stale and `orcann status` names which setting moved. `--sweep` writes
its montage and table beside the recordings, extracting nothing.

Written by: segment.   Read by: activity.

This folder ships empty to preserve the directory structure so the tree survives a browser
upload to GitHub. Contents stay local and are not committed; delete this placeholder
once the folder holds real files.
